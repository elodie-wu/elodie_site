# 历史美术资源

这里保留已被新版替换的 Work / Logs / About 背景、旧首页主图和重复字体文件。它们从 `public/` 移到此处，不再随网站发布，没有直接删除。

当前背景配置统一在 `src/config/site.ts`。要恢复旧图，先复制到 `public/assets/`，再修改配置中的相对路径；不要在前端引用本归档目录或 E 盘绝对路径。

当前使用的 SonyCam 字体仍在 `src/assets/fonts/`；授权文件在 `public/licenses/sonycam-original/`。仓库外的 `../extracted_site_assets/` 源素材没有改动。
